# =================================================================
# ROOT .env FILE - For Docker Compose Only
# =================================================================
# This file is ONLY used when running with Docker Compose.
# For local development (without Docker), see backend/.env.example
#
# NOTE: MONGODB_URI is not needed here - it's hardcoded in 
# docker-compose.yml for the Docker internal network.

# Google API Key for AI/ML services
GOOGLE_API_KEY=your_google_api_key_here
